{namespace FormValidator}

/**
 * This renders the main element.
 * @param? elementClasses
 */
{template .render}
	<div class="form-validator{$elementClasses ? ' ' + $elementClasses : ''}">
	</div>
{/template}
